// Funciones anonimas o Funciones de tipo expresión.

// Declaración de una función de tipo Expresión(Anónima)
let sumar = function (a,b){return a + b};
let resultado = sumar(1,2);
console.log("Suma : " + resultado);